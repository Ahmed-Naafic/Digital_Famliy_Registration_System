import 'package:flutter/material.dart';

/// App-wide constants for colors, text sizes, and spacing
/// This file centralizes all design constants to maintain consistency across the app

// ============================================================================
// COLOR CONSTANTS
// ============================================================================

/// Primary color - Dark blue used for main UI elements like AppBar and primary buttons
const Color kPrimaryColor = Color(0xFF0D47A1);

/// Secondary color - Lighter blue used for secondary actions and highlights
const Color kSecondaryColor = Color(0xFF1976D2);

/// Accent color - Amber/yellow used for important call-to-action elements
const Color kAccentColor = Color(0xFFFFC107);

/// Background color - Light grey used as the default background for screens
const Color kBackgroundColor = Color(0xFFF5F5F5);

/// Dark background color - Dark blue/black for dark theme
const Color kDarkBackgroundColor = Color(0xFF0A0E27);

/// Dark card color - Slightly lighter than background for cards
const Color kDarkCardColor = Color(0xFF1A1F3A);

/// Success color - Green for positive indicators
const Color kSuccessColor = Color(0xFF4CAF50);

/// Error color - Red used for error messages and validation errors
const Color kErrorColor = Color(0xFFD32F2F);

// ============================================================================
// TEXT SIZE CONSTANTS
// ============================================================================

/// Heading font size - Used for main titles and section headers
const double kHeadingFontSize = 24.0;

/// Subheading font size - Used for secondary titles and subsection headers
const double kSubheadingFontSize = 18.0;

/// Body font size - Used for regular text content and paragraphs
const double kBodyFontSize = 14.0;

// ============================================================================
// SPACING CONSTANTS
// ============================================================================

/// Default padding - Standard padding value used throughout the app for consistent spacing
const double kDefaultPadding = 16.0;

